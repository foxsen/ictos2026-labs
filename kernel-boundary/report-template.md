# 跨越保护边界实验报告

**所有学生完成基本部分；完成进阶实验时再填写 B 部分，seL4 仅填可选附录。** 每个数字注明原始文件及记录；不另交假设卡。不以共享内存一定更快作为结论要求。

## A. 基本要求：Linux 测试与报告

### A1. 环境、版本与参数

- 姓名 / 学号、小组成员与分工：
- 实验包版本或校验记录、代码修改：
- CPU、Linux 内核、编译器、是否使用容器：
- 固定 CPU、频率策略、安全缓解状态（引用 environment.txt）：

| 项目 | 默认值 | 实际值 / 文件位置 |
|---|---|---|
| Linux 调用 | 101 批 × 10000 次；固定同一 CPU | |
| Linux socket | 每长度 51 批 × 1000 次往返；双进程同核 | |
| 有效载荷长度 | 1、64、1024、4096 B | |
| 输出 | syscall.csv、ipc.csv、summary.csv、environment.txt、stderr | |

说明预热、任何参数修改和重跑原因。默认调用预热为每类 iterations/10+1 次，socket 为每长度 50 次往返。保存异常日志，不只选择最快的一次。

### A2. L1：Linux 调用耗时

填入 `summary.csv` 的对应统计。单位为 **ns/操作，统计对象为批次均值**，不扣除基线。

| benchmark | 批次数 | median | mean | P95 |
|---|---:|---:|---:|---:|
| empty_loop | | | | |
| ordinary_function | | | | |
| raw_getpid | | | | |
| clock_gettime_vdso_candidate | | | | |
| raw_clock_gettime | | | | |

- 原始文件及记录定位：
- 两种 clock_gettime 的 median 差值（raw − libc）及比值（raw / libc）：
- 对照 `bench_syscall.c`，说明普通函数、raw syscall 与 libc 取时接口的调用方式：
- 结合数据说明差异。没有动态路径验证时，将 libc 调用标作“vDSO 候选路径”：

### A3. L2：socket 消息长度

插入“消息字节数—median ns/往返”曲线，并填表。一次操作包括客户请求与服务回复的完整往返。

| 消息长度（B） | 批次数 | median ns/往返 | P95 ns/往返 |
|---:|---:|---:|---:|
| 1 | | | |
| 64 | | | |
| 1024 | | | |
| 4096 | | | |

- 图与原始数据位置：
- median 差值（4096 B − 1 B）及比值（4096 B / 1 B）：
- 曲线趋势：
- 对照 `bench_ipc.c`，说明一次往返的收发过程，以及消息传输、系统调用和调度对耗时的影响；无法单独量化的成本明确标出：

### A4. 结论与复现

用三至五条结论归纳观察，注明适用配置。区分代码明确执行的步骤、数据观察以及尚未验证的解释。不能把两种不同操作的差值直接称为纯模式切换成本。

列出环境准备、运行、汇总和制图命令，附图表生成脚本或可编辑表格。完成 A 部分即满足基本提交要求。

## B. 进阶：实现与评价共享内存通信

未做进阶时整节略过。具体任务见 ADVANCED.md。

### B1. 实现与协议（约一页）

- 实现文件、与 starter 的差异、额外编译选项：
- 选择的进程间通知机制及理由：
- 状态表或状态转换图；字段由谁写、谁读：
- 一次请求—回复经过的发布、等待、唤醒和复制位置：
- 请求 / 回复缓冲区何时可复用，如何防止读到未完成的数据：
- 内存顺序或锁的作用；通知先于等待时如何避免丢失唤醒：
- 等待中断、条件不满足和系统调用失败的处理：

### B2. 正确性与配置

- 正确性运行目录、run.json、源码 / 二进制哈希：
- 同核 / 异核 × 四种长度 × 三个 seed 的检查结果；失败记录与修复说明：
- 自行增加的测试（如非标准长度、更多轮次，可选）：
- 为什么通过测试仍不能代替协议正确性说明：

| 布局 | 客户 CPU | 服务 CPU | 物理核 / SMT / LLC / NUMA 关系 | 测量参数 |
|---|---:|---:|---|---|
| same | | | | |
| different | | | | |

只有一个可用 CPU 时写明限制，保留同核结果，在可用环境补做异核；不编造缺失数据。正式计时前必须通过检查。记录实际采样数、预热、seed、超时和重复运行情况。

### B3. 对照数据

同一框架内比较 socket 与自己的共享内存实现。两者有效载荷之外均有 8 B 序号头；不能把基础 L2 的旧结果作为此处的 socket 数据。

| 布局 | 有效载荷 B | socket median ns/往返 | SHM median ns/往返 | socket P95 | SHM P95 | SHM−socket | SHM/socket |
|---|---:|---:|---:|---:|---:|---:|---:|
| same | 1 | | | | | | |
| same | 64 | | | | | | |
| same | 1024 | | | | | | |
| same | 4096 | | | | | | |
| different | 1 | | | | | | |
| different | 64 | | | | | | |
| different | 1024 | | | | | | |
| different | 4096 | | | | | | |

- 所有统计均为**批次均值统计**；注明各组样本数与原始 CSV。
- 插入同核 / 异核两张曲线，每张同时画 socket 和共享内存。
- 对应 summary.csv、comparison.csv、图表生成命令：

### B4. 机制评价与论文联系

- 随消息长度、CPU 布局变化，观察到什么？引用具体数据：
- 根据自己的实现，哪些内核复制或系统调用减少？哪些用户态复制、通知、调度或跨核成本仍存在或增加？
- 哪些解释能由代码确认，哪些成本不能由本次测量独立分离？
- 如果额外跟踪路径，列出独立日志；跟踪结果不混入正式计时：
- 联系一条课程论文论断（不超过一页）：注明出处、自己的对应数据、条件差异，以及尚需增加什么测量：

### B5. 复现与提交

提交学生源码及差异、完整进阶结果目录（含 source/、run.json、原始检查日志和 CSV）、绘图材料与复现命令。共享内存较慢也可以构成完整结论。

## C. 可选附录：seL4

仅选做时填写。准备步骤、版本、记录选择见 sel4/README.md；不影响 A、B 的完成。

- profile、manifest、预构建包及校验结果或源码构建命令：
- 两个镜像除 fastpath 开关之外的配置一致性：
- on/off 的 serial.log、results.json、ipc.csv、pinned-manifest.xml 位置：


### C1 六组统计

仅选 `One way IPC microbenchmarks` 中普通 `seL4_Call`、`client->server` 的记录。核对 JSON 中双方优先级一致；不选 `(FPU)` 行。单位为 **TCG cycle，单向 IPC，经基准计时开销校正**。

- JSON 中 Client Prio / Server Prio 的实际值：
- 结果来源与所选记录定位：

| fastpath | same_vspace | IPC length（words） | samples | median | Q1 | Q3 |
|---|---|---:|---:|---:|---:|---:|
| on | True | 0 | | | | |
| on | False | 0 | | | | |
| on | False | 10 | | | | |
| off | True | 0 | | | | |
| off | False | 0 | | | | |
| off | False | 10 | | | | |

### C2 配对计算

A、B 取对应记录的 median；A 为零时比值填“不适用”，保留原始值。

| 对照 | A 配置 | B 配置 | A | B | B−A | B/A |
|---|---|---|---:|---:|---:|---:|
| S1 | on / 同空间 / 0 | on / 异空间 / 0 | | | | |
| S2 | on / 异空间 / 0 | on / 异空间 / 10 | | | | |
| S3a | on / 异空间 / 0 | off / 异空间 / 0 | | | | |
| S3b | on / 异空间 / 10 | off / 异空间 / 10 | | | | |

### C3 结果说明

每项先描述变化，再结合机制解释；未验证的解释注明是推测。

- S1：同 / 异地址空间的变化，以及 TCG 下允许解释的范围：
- S2：短 / 长消息基准配置的变化；不将差值直接等同于纯复制成本：
- S3a/S3b：关闭快路径对两种长度的影响，结合快路径适用条件说明；区分“开关开启”和“实际命中”：


TCG cycle 不是宿主 CPU 周期，不用于定量推断真实 Cache/TLB 成本。这里的单向 IPC 不与 Linux 纳秒 / 完整往返作跨系统绝对排名。

## 提交检查

- [ ] 基本：L1 五行、L2 四个长度和曲线，机制说明齐全。
- [ ] 基本：环境、原始数据、命令、版本与绘图材料齐全。
- [ ] 进阶（如做）：学生实现、协议说明、检查日志、同 / 异核对照与评价齐全。
- [ ] seL4（如做）：独立附录中六组统计、四对计算及解释齐全。
- [ ] 所有表格注明单位、操作定义与统计对象；未做的部分如实标明。
